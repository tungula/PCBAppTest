import React from 'react';
import {
   View,
   Button,
   Text,
   TextInput,
   FlatList,
   TouchableOpacity,
   StyleSheet,
   SafeAreaView,
} from 'react-native';

interface Props {}

export const MyStatistics: React.FC<Props> = ({}) => {
   return (
      <SafeAreaView>
         <Text>Statistics</Text>
      </SafeAreaView>
   );
};
