import React from 'react';
import {
   View,
   Button,
   Text,
   TextInput,
   FlatList,
   TouchableOpacity,
   StyleSheet,
   SafeAreaView,
} from 'react-native';

interface Props {}

export const ModalContainer: React.FC<Props> = ({}) => {
   return (
      <SafeAreaView>
         <Text>DD</Text>
      </SafeAreaView>
   );
};
